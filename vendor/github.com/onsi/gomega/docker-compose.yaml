# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0

version: '3.0'

services:
  test:
    build:
      dockerfile: Dockerfile
      context: .
    working_dir: /app
    volumes:
      - ${PWD}:/app
